[pytest]
testpaths = backend/tests
pythonpath = backend
asyncio_mode = auto
# Only collect our integration tests to avoid import path collisions with other test modules
python_files = test_integration_*.py
